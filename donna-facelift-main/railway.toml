# NOTE: Imported from phase5-production-ready for reference.
# Review and update environment variables before use.
[build]
builder = "nixpacks"

[deploy]
startCommand = "cd websocket-server && npm start"
restartPolicyType = "always"

[env]
NODE_ENV = "production"
