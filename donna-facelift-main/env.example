# Sentry Configuration
NEXT_PUBLIC_SENTRY_DSN=https://your-dsn@sentry.io/project-id
SENTRY_DSN=https://your-dsn@sentry.io/project-id
NEXT_PUBLIC_APP_VERSION=1.0.0
APP_VERSION=1.0.0

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_REALTIME_MODEL=gpt-4o-realtime-preview-2024-10-01
# Optional: model for /api/knowledge-chat (GTM / ICP / investor / product markdown). Defaults to gpt-4o-mini.
OPENAI_KNOWLEDGE_CHAT_MODEL=gpt-4o-mini

# ElevenLabs Configuration
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
DEFAULT_VOICE_ID=XcXEQzuLXRU9RcfWzEJt
VOICE_MODEL=eleven_multilingual_v2

# Gmail Configuration
GMAIL_CLIENT_ID=your_gmail_client_id
GMAIL_CLIENT_SECRET=your_gmail_client_secret
GMAIL_REFRESH_TOKEN=your_gmail_refresh_token

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=your_smtp_password
SMTP_ENCRYPTION=tls
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_FROM_NAME=DONNA

# System Configuration
DOMAIN_NAME=localhost
ENVIRONMENT=development
DEBUG_MODE=true
NEXT_PUBLIC_API_BASE=http://localhost/donna
NEXT_PUBLIC_WEBSOCKET_URL=ws://localhost:3001/realtime

# Auth: Demo mode uses cookie-based session (donna_demo_session) from /sign-in page

# Supabase Configuration
# Public URL and anon key for browser
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only URL and service role key
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Security / CORS
# Secret used for internal API auth between services (do not share)
API_SECRET=
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:3000

# Google OAuth (Next.js Gmail integration)
# Use these for OAuth-based Gmail; tokens will be stored in Supabase
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# For local dev, something like: http://localhost:3000/api/gmail/oauth/callback
GOOGLE_REDIRECT_URI=

# Telnyx Configuration
TELNYX_API_KEY=your_telnyx_api_key
TELNYX_MESSAGING_PROFILE_ID=your_messaging_profile_id
TELNYX_PHONE_NUMBER=+1234567890
TELNYX_WEBHOOK_SECRET=your_webhook_secret

# Provider Selection
VOICE_PROVIDER=telnyx
MESSAGING_PROVIDER=telnyx
