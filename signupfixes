You are working in the aidonna.co landing page repository. Stack: Next.js with
NextAuth (provider id "cognito", OAuth code flow) and Supabase as the database.
Login goes through the AWS Cognito hosted UI:
- Cognito domain: donna-production.auth.us-east-1.amazoncognito.com
- App client id: pnjqk91v6gmtskghdmm3m6uho
- Callback: https://aidonna.co/api/auth/callback/cognito
Do NOT change any Cognito/AWS settings - those are managed by another team.
Only change code in this repository (and tell me if a Supabase dashboard step
or a new environment variable is needed - list it explicitly at the end).
Keep changes small and focused. Fix these four issues:

ISSUE 1 (most important - blocks every new user):
After a user logs in, the onboarding page fails and shows text like
"ask the administrator to apply a supabase migration".
1. Search the codebase for that message to find the code path that produces it.
2. Find the real underlying error. Check what Supabase query fails there.
   It is one of two cases:
   CASE A: a table or column is missing in production (error like
   'relation ... does not exist'). If so: make sure all schema changes exist as
   files in supabase/migrations/, and add a package.json script
   "deploy": "supabase db push && vercel --prod"
   so migrations always run before deploys. Tell me the exact one-time
   terminal commands I need to run to apply the pending migrations now
   (supabase link + supabase db push), with placeholders filled in from
   this project's config where possible.
   CASE B: the queries are blocked by Row Level Security. This project used
   to use Supabase Auth but now uses Cognito via NextAuth, so auth.uid() is
   NULL and RLS policies based on it block everything. If so: move all user
   data reads/writes to server-side code (route handlers or server actions)
   using the SUPABASE_SERVICE_ROLE_KEY, and protect every one of those calls
   by checking the NextAuth session first (getServerSession). Identify rows
   by the Cognito "sub" claim from the session. NEVER use the service role
   key in client-side code. Tell me to add SUPABASE_SERVICE_ROLE_KEY in
   Vercel env vars if it is not already there.
3. Whichever case it is, replace the user-facing message with a friendly
   generic error ("Something went wrong on our end - please try again in a
   few minutes") and log the real error server-side with console.error so it
   shows in Vercel logs.

ISSUE 2 (confirmation code page shows false errors):
When a user confirms their email code successfully but the form gets
submitted again (refresh, back button, double click), Cognito returns
"User cannot be confirmed. Current status is CONFIRMED" and the user sees a
red error even though their account is fine.
1. Find the signup/confirmation handling in this repo.
2. Treat the error message containing "Current status is CONFIRMED" as
   SUCCESS: redirect the user to the login step with a message like
   "You're all set - please sign in."
3. Disable the confirm submit button after the first click, and redirect
   after success so refresh/back cannot resubmit the form.

ISSUE 3 (users bounced to a Cognito error page during login):
The app sometimes restarts the OAuth login with an already-used state
parameter, which sends users to Cognito's error page (we see repeated
Error_GET 400 events server-side).
1. Find every place that calls signIn("cognito") or redirects to the
   Cognito authorize URL.
2. Before starting login, check whether a session already exists
   (getServerSession or useSession). If it does, redirect to the app's
   main page instead of starting OAuth again.
3. Make sure nothing automatically triggers signIn() when the OAuth
   callback page or an error page loads (check layouts, middleware, and
   useEffect hooks).
4. When the OAuth callback fails, redirect the user to /login fresh -
   never send them back to the previous URL.

ISSUE 4 (login page promises things that do not exist, and there is no
signup page):
1. The /login page says "Sign in with Google, email and password, or a
   magic link." Google login and magic links are NOT configured. Change the
   text to only mention email and password.
2. /signup currently returns 404. Create a /signup page, and add a visible
   "Create account" button on the homepage and on /login. The simplest
   correct implementation: link "Create account" to this exact URL:
   https://donna-production.auth.us-east-1.amazoncognito.com/signup?client_id=pnjqk91v6gmtskghdmm3m6uho&response_type=code&scope=openid+email+profile&redirect_uri=https%3A%2F%2Faidonna.co%2Fapi%2Fauth%2Fcallback%2Fcognito
3. If a login attempt fails because the account exists but was never
   confirmed (UserNotConfirmedException), show the user a "resend
   confirmation code" option instead of a generic error.

When you are done, give me:
- A plain-English summary of what you changed, file by file.
- Any terminal commands I must run, in order, copy-pasteable.
- Any environment variables I must add in Vercel, with exact names.
- Anything you could not fix from code alone and what to ask for.